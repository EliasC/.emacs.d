# name : for(...; ...; ...) { ... }
# for(unsigned int ${1:i} = 0; $1 < $2; ${++$1}) {
#    $0
# }
# name: for (...; ...; ...) { ... }
# key: for
# --
for (${1:int i = 0}; ${2:i < N}; ${3:++i}){
    $0
}